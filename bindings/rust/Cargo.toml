[package]
name = "zkmetal-rs"
version = "0.1.0"
edition = "2021"
description = "zkMetal GPU-accelerated ZK library bindings"
authors = ["carni-ships"]

[dependencies]
zkmetal-sys = { path = "zkmetal-sys", features = ["gpu", "neon"] }

[features]
default = ["gpu"]
gpu = []
neon = []

[workspace]